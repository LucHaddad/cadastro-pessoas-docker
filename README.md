# Trabalho 2 - Docker Compose Multi-container

Aplicação com Frontend (Nginx), Backend REST (Node.js/Express) e Banco de Dados (PostgreSQL).

## Como Executar
1. `docker compose up -d --build`
2. Acesse http://localhost:8080
